requests>=2.31.0
PyMuPDF>=1.23.0
pyyaml>=6.0
pydantic>=2.5.0
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0

# Optional: install only if you need the corresponding feature.
# sentence-transformers>=2.2.0   # Better embeddings (falls back to TF-IDF without it)
# matplotlib>=3.7.0               # Plotting (strength heatmap, argument graph)
# networkx>=3.0                   # Argument graph visualization
# b2sdk>=2.0.0                    # Backblaze B2 cloud storage (only for `ingest --upload-b2`)
# playwright>=1.40.0              # Browser-driven PDF harvest (`systematic_review browser-harvest`); then run `playwright install chromium`
# pyarrow>=14.0.0                 # Embeddings parquet I/O for methodological-audit clustering
